访问者模式
就是单纯的实现一个多对多的关系
总结一个抽象类作为访问者
访问者可以拥有多种的实现方式，同时一个访问者可以访问多种不同的元素
即n->n
但是这里有一个区别就是，访问者模式的示例代码设定针对不同实现元素的访问拥有独自不同的访问方法实现

说实话我不太理解，他把访问不同的元素写成了不同的方法，这个直接在元素内定义如何访问不就好了吗。
他这样的话如果有N个元素岂不是要定义N个方法？
我觉得不太行
然后不同访问者的访问方式就由各自具体实现的访问者实现就好了。
我个人认为上访问者定义一个统一的访问方法，元素也定义一个统一的被访问的方法(实际上这个模式元素就是各自实现被访问的方法)
然后访问者针对不同的访问对象自己在内部函数写判断就行了，他这个把元素对象的判断写到最外面，我觉得不太行，因为这样就必须要
提前把所有的方法都给定义出来，如果碰到实时项目你需要添加什么方法，这十分不灵活，不利于工程搭建