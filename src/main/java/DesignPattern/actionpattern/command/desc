命令模式，按照说明的意思是，请求有发送方和处理方，将发送方和处理方分开，进行分别建模，有利于解耦，增强程序鲁棒性
我感觉这个和前后端分离有点类似的概念，前端发送请求，后端处理请求。
但是好像不是这样的。。。
代码显示的是，发送方持有命令，命令可以通过多种形式实现。
同时命令持有处理者，处理者也可以通过多种形式实现，这样一来
发送命令的一方可以通过多种方式发送命令
命令也可以通过多种方式来执行
类似1(发送命令)->n(n种命令)->n(n种执行方法)//这里的N指的是前一种对象对后一种对象的拥有数量(抽象上的数量理解)