java类加载过程
准备过程:
加载class类文件
验证class文件是否存在错误，是否符合JVM规范
准备空间，为新创建的类准备内存空间，将变量赋上默认的值,0,false,null...
解析，将常量池内的符号引用修改为直接引用，编译时java不知道类的实际地址，用符号代替，加载后修改为直接地址

初始化:执行类的初始化操作，执行默认的赋值语句和static语句

延伸：
spring类加载过程
实际上就是，spring读取你的配置文件，然后开启spring容器，spring容器通过扫描你的包里的文件里的注解，通过反射的方式将
被扫描到的类加载创建，并由spring这个容器来进行这些bean的管理，创建，使用，销毁。(IOC的本质)

