mvcc    multi-version concurrency control多版本控制并发
是mysql innodb引擎

MVCC指多版本并发控制，事实上，每个事务都会确定当前读的版本，之后无论你如何操作，只要在该事物内，你读操作返回的数据永远都是一样的
并且，你在springboot上使用transacational注解事实上也是MVCC，即读的操作都是读的同一版本。
即事实上不会产生幻读....
但是会发生一些奇妙的操作，比如你先查找某个不存在的行数，然后过一会儿在对这行进行修改并查询，如果你在修改前用其他事务新添加了
这行数据，你其实是可以修改并在后面的查询中查到你修改的数据。。。。不过添加这行数据的其他数据不会被查到，只有你修改的部分能在
本次的事务中看到的数据只有本次事务修改的数据